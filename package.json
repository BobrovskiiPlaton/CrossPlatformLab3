{
  "scripts": {
    "start": "parcel index.html",
    "test": "jest",
    "coverage": "jest --collect-coverage",
    "build": "parcel build index.html --dist-dir docs --no-source-maps --public-url ./",
    "api": "node api-server.js",
    "dev": "concurrently \"npm run api\" \"parcel index.html\""
  },
  "devDependencies": {
    "@babel/core": "^7.19.1",
    "@parcel/babel-preset-env": "^2.7.0",
    "babel-jest": "^29.0.3",
    "concurrently": "^7.6.0",
    "jest": "^29.0.3",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "d3": "^7.6.1",
    "express": "^4.21.2"
  }
}
